# Walkthrough: Archiving Completed Plans & Clarifying Ticket Locations

## What Changed
1. **Disambiguation**: Clarified the boundary between architectural planning documents (`docs/planning/`) and executable tickets (`openspec/changes/dual-cashflow-budgeting/tickets/`).
2. **Planning Archive**: Moved all completed `✅_*.md` implementation plans from `docs/planning/` into `docs/planning/archive/` per `AGENTS.md` Section 4.
3. **Clean Workspace**: `docs/planning/` is now reserved exclusively for active, in-progress planning passes.

## Verification
- `openspec/changes/dual-cashflow-budgeting/tickets/`: Contains all 7 active tracer-bullet tickets (`01-core-dual-ledger.md` through `07-month-cycle-paging.md`).
- `docs/planning/`: Cleaned up with all 32 historical plans archived in `docs/planning/archive/`.
